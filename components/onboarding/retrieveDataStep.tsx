const RetrieveDataStep = () => {
  //   const { showError } = useNotification();
  //   const { data, customListsData, loading, error } = useInitialization();
  //   const { setLoading, customLists, setMediaData, setCustomLists, nextStep } =
  //     useOnboarding();

  //   useEffect(() => {
  //     if (!customLists && data && customListsData && !error) {
  //       setMediaData(data);
  //       setCustomLists(
  //         customListsData.User.mediaListOptions.mangaList.customLists
  //       );
  //       nextStep();
  //     }
  //   }, [data, customListsData]);

  //   useEffect(() => {
  //     setLoading(loading);
  //   }, [loading]);

  //   useEffect(() => {
  //     error && showError(error, 'Unable to retrieve data from AniList');
  //   }, [error]);

  return null;
};

export default RetrieveDataStep;
